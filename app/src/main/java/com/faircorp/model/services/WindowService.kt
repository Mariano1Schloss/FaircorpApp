package com.faircorp.model.services

import com.faircorp.model.Dto.RoomDto
import com.faircorp.model.Dto.Status
import com.faircorp.model.Dto.WindowDto

class WindowService {

    /*companion object {
        // Fake windows
        val WINDOWS: List<WindowDto> = listOf(
            WindowDto(1, "Entry Window", RoomService.ROOMS[0], Status.CLOSED),
            WindowDto(2, "Back Window", RoomService.ROOMS[0], Status.CLOSED),
            WindowDto(3, "Sliding door", RoomService.ROOMS[1], Status.OPEN),
            WindowDto(4, "Window 1", RoomService.ROOMS[2], Status.CLOSED),
            WindowDto(5, "Window 2", RoomService.ROOMS[2], Status.CLOSED),
        )

    }

    fun findById(id: Long) = WINDOWS.firstOrNull { it.id == id}

    fun findAll() = WINDOWS.sortedBy { it.name }


*/

}